/**
 * Create a function that takes a sorted array of number as input and returns the number of uniqe values fund in that array
 */

const countUniqueValues = () => {};

// TESTS
countUniqueValues([1, 2]); // returns 2
countUniqueValues([]); // returns 0
countUniqueValues([1, 1, 1, 1, 1, 1]); // returns 1

// TYPESCRIPT VERSION
